# WAR.MARKET Assets

This folder contains visual assets for the WAR.MARKET mockup pages.

## Video Placeholder

Add your 10-second video file here when ready. Expected format:
- Filename: `hero-video.mp4` (or similar)
- Duration: 10 seconds
- Format: MP4 (WebM as fallback recommended)
- Sequence: Map → Stress → UI → Logo

To use, replace the video placeholder div in `index.html` with:
```html
<video autoplay loop muted class="hero-video">
  <source src="assets/hero-video.mp4" type="video/mp4">
</video>
```

## Map Overlay

`map-overlay.svg` - World map silhouette with trade corridors and data nodes for background visualization.

